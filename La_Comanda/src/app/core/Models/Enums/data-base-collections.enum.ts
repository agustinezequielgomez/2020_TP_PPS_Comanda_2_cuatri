export enum DataBaseCollections {
  config = 'config',
  users = 'users',
  devices = 'devices',
  mesas = 'mesas',
  comidas = 'comidas',
  bebidas = 'bebidas',
  waiting_list = 'waiting_list',
  orders = 'orders',
}
